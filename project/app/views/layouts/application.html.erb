<!DOCTYPE html>
<html>
  <head>
    <title>ProyectoGrupo64</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <% if user_signed_in? %>
      <%= render "/navbar" %>
  <% end %>
  <body>
    <%= yield %>
  </body>
</html>
