<h1>Request#index</h1>

<p>Find me in app/views/request/index.html.erb</p>

<% request_user = @requests.where(user_id: current_user.id)%>
<% request_user.each do |request| %>
    <h2>Grupo: <%= Group.find(request.group_id).name %></h2>
    <h2>Usuario: <%= User.find(request.user_id).name %></h2>
    <h2>Estado: <%= request.status %></h2>
    <h2>Descripción: <%= request.description %></h2>
    <%= button_to "Cancelar solicitud", request, method: :delete %>
    <% end%>

